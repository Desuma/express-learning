export enum E_EXPRESS_PARAMS {
  AppPort = 8080,
  MySQLPort = 3000,
  MySQLHost = 'localhost',
}

export enum E_EXPRESS_DEV_PARAMS {
  AppPort = 8080,
  MySQLHost = 'localhost',
  MySQLPort = 3000,
  MySQLName = 'root',
  MySQLPassword = 'password',
}