export enum E_EXPRESS_PARAMS {
  /**
   * 应用启动端口号
   */
  AppPort = 8080,
  /**
   * 数据库连接端口号
   */
  MySQLPort = 3000,
  /**
   * 数据库连接地址
   */
  MySQLHost = 'localhost',
}

export enum E_EXPRESS_DEV_PARAMS {
  /**
   * 应用启动端口号
   */
  AppPort = 8080,
  /**
   * 数据库连接地址
   */
  MySQLHost = 'localhost',
  /**
   * 数据库连接端口号
   */
  MySQLPort = 3000,
  /**
   * 数据库连接用户名
   */
  MySQLName = 'root',
  /**
   * 数据库连接密码
   */
  MySQLPassword = 'password',
}